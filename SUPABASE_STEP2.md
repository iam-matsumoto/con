# Ver.8 Step2：予定データをSupabaseへ保存する

## 1. SQLを実行する

1. Supabaseのプロジェクトを開く
2. 左側の「SQL Editor」を開く
3. `supabase/step2_schedules.sql` の中身をすべてコピーする
4. SQL Editorへ貼り付ける
5. 「Run」を押す

## 2. アプリを再起動する

```powershell
Ctrl + C
npm run dev
```

## 3. 既存予定をクラウドへ移す

1. 管理者でログインする
2. 「設定」を開く
3. 「クラウド同期（Supabase）」まで下へ移動する
4. 「このPCの予定をクラウドへ移行」を押す
5. 確認画面でOKを押す

移行後は、予定の追加・編集・削除がSupabaseへ保存されます。
別のPCでは同じ `.env.local` を設定したあと、「クラウド予定を読み込む」を押してください。

## 注意

Step2では、Supabase Authへ切り替える前のため、予定テーブルだけ一時的に公開可能キーから変更できるポリシーを使用します。URLと公開可能キーを知っている人が予定を変更できる状態なので、インターネット公開はまだ行わないでください。認証対応版でこの一時ポリシーを削除します。
