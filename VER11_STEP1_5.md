# Ver.11 Step1.5 — Realtime同期

今回の変更は **予定と個人通知のRealtime同期のみ** です。

## 追加内容

- PCで予定を追加・編集・削除すると、別端末のカレンダーも自動更新
- 対象社員への個人通知が、再読み込みなしで自動表示
- 通知の既読状態も別タブ・別端末へ反映
- 外部業者、図面、PWAなどには今回触れていません

## 最初に1回だけ

Supabase SQL Editorで次を実行してください。

`supabase/ver11_step1_5_realtime.sql`

## 起動

`.env.local` を前のStep1.4からコピーしてから:

```powershell
npm install
npm run dev
```

今回 `@supabase/supabase-js` を追加したため、Step1.4から更新する場合も `npm install` を一度実行してください。

## 動作確認

1. PCとスマホで別々のユーザーとしてログイン
2. 両方で画面を開いたままにする
3. 管理者PCから予定を追加
4. スマホ側のカレンダーが再読み込みなしで更新されることを確認
5. スマホ側のお知らせに個人通知が再読み込みなしで表示されることを確認
